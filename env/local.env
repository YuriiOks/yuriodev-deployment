# local: public, non-secret backend config (tracked). Secrets go in env/local.secrets.env (gitignored).
ENVIRONMENT=local
API_TITLE="YuriODev Website API (local)"
API_VERSION="0.1.0"
CORS_ORIGINS=["http://localhost:5173"]
LOG_LEVEL=DEBUG
